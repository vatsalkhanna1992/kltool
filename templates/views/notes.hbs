<!DOCTYPE html>
<html>
    <head>
        <title>
            Notes | Kltool
        </title>
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
   </head>
    <body class="note-board">
        <div class="container">
            {{>header}}
            <div class="row">
                <div class="col s12">
                    <h5 class="page_title">
                        <span>Notes</span>
                        <a href="/notes" title="Add Note"><i class="material-icons dp48">note_add</i></a>
                        <a href="" class="delete-note" title="Delete Note"><i class="material-icons dp48">delete</i></a>
                    </h5>
                    <div class="message">{{message}}</div>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-3">
                    <div class="notes">
                        {{#each notes}}
                            {{>note}}
                        {{/each}}
                    </div>
                </div>
                <div class="col-sm-9">
                    <form class="add-note">
                        <input type="text" name="note_title" id="note_title" placeholder="Note title" maxlength="100" required />
                        <div id="note_description" name="note_description"></div>
                        <p></p>
                        <button type="submit" class="kltool-button save-note btn deep-purple darken-1 right">Save note</button>
                    </form>
                </div>
            </div>
        </div>
        {{>general_css_js}}
        {{>materialize}}
        <link href="/quill/quill.snow.css" rel="stylesheet">
        <script src="/quill/quill.min.js"></script>
        <script>
            var quill = new Quill('#note_description', {
                placeholder: 'Write a note...',
                theme: 'snow'
            });
        </script>
    </body>
</html>